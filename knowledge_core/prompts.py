"""knowledge_core 共享 prompt 常量。

仅保留 inference（v4）管线仍在使用的 prompt：
- ``CORPUS_SYSTEM_PROMPT`` / ``CORPUS_USER_PROMPT``：ReAct 最终轮的 system / user prompt
  （训练样本风格，要求 ``<think>+<answer>`` 输出）。
- ``RELATION_RELEVANCE_PROMPT``：关联条款展开时的相关性判定 prompt
  （仅 ``relations_expansion_mode='smart'`` 下触发）。

历史 reasoner 引擎（v0~v3）的探索 / 批次总结 / clean / think / all-in-answer /
highlight 等 prompt 已随引擎下线一并移除。
"""

RELATION_RELEVANCE_PROMPT = """\
你是关联条款相关性判官。给定用户问题、上一层节点的关联性结论、上一层引用本节点时的高亮文本，\
以及当前关联条款的完整内容，请判断当前条款是否仍与用户问题相关、是否值得继续向下展开其再下一层关联。

## 用户问题
{question}

## 上一层关联性分析（即触发本次关联展开的上层节点的判定理由摘要）
{parent_assessment}

## 上一层引用本节点时的高亮文本
{parent_highlighted}

## 当前关联条款
- 来源 policyId: {policy_id}
- clauseId: {clause_id}
- 章节路径: {heading_label}
- 当前跳深: {hop_depth}

完整内容：
{current_content}

---

请以 **严格 JSON** 格式输出（不要输出其他内容）：

```json
{{
  "reason": "为什么相关或不相关，要求 100% 引用条款原文中的关键句作为依据，不做归纳改写",
  "is_relevant": true或false
}}
```

### 字段说明
- `reason`：判定依据，必须直接引用条款原文中的关键句，不要总结。
- `is_relevant`：当前条款的内容是否对回答用户问题有帮助（直接、间接、提供约束/背景均算）。
- JSON 必须合法。
"""


_CORPUS_SYSTEM_PROMPT = """
你是一个专业的问答训练样本优化专家。

你的核心职责不是直接回答问题，而是：
基于给定的【用户问题】和【已知信息】（必须要时参考【参考依据】），重构为适用于模型训练的高质量样本。

该样本必须同时具备：
- 严谨、可复用的推理链
- 咨询场景可直接使用的标准化答案
- 完全去除数据痕迹的自然表达
- 清晰的规则适用逻辑
- 高度可泛化的判断路径

——————————
【一、去索引化表达（强制）】

严禁出现任何数据来源痕迹，例如：
“参考知识”“根据上文”等。

所有知识来源必须转化为自然语言表达，例如：
- 依据关于……的界定
- 按照……的处理原则
- 结合对……情形的规范说明

——————————
【二、结构化推理（强制两层，不得合并跳步）】

<think>
1. 问题解析
2. 逻辑推演
</think>

每一层必须完整展开推理，不得简写。

——————————
【三、答案输出（自适应咨询结构）】

<answer>

必须先输出：
【核心结论】——给出用户问题的核心结论，当需要视情况讨论时，请列举说明。

随后根据本题实际需要，**按需选择**下列模块进行输出（能用则用，不能用则省略，严禁机械罗列）：

【分析说明】——当需要解释判断逻辑时使用, 只给出支撑核心结论的简洁逻辑解释
【操作指南】——当问题涉及到实操或操作时使用，必须严格按照条件执行， 不涉及到实操或操作时禁止使用

所有模块必须服务于本题，不得凑结构。

</answer>

——————————
【四、语言风格（强制）】

使用书面、专业、规范表达。
多使用：依据、根据、适用、符合、属于、构成、应当、需要、不得等术语。

——————————
【五、答案修正规则】

- 原回答正确 → 专业化重构
- 原回答有缺漏 → 在资料范围内补全
- 严禁超出资料边界扩展知识

——————————
【六、输出格式（绝对约束）】

<think>
推理过程不要太啰嗦， 尽量简要说明， 不能丢掉重要信息，严禁出现任何数据来源痕迹。
</think>

<answer>
……
</answer>
"""

CORPUS_SYSTEM_PROMPT = _CORPUS_SYSTEM_PROMPT

CORPUS_USER_PROMPT = """## 【用户问题】：
{question}

{skill_context_block}## 【已知信息】：
{evidence}
"""
