# LanceDB 表存放目录（容器内为 /app/data；本地裸跑可设为 ./data）
STORE_DIR=./data

# 客户端鉴权 key（X-API-Key 或 Authorization: Bearer）
API_KEY=changeme

# uvicorn 监听
HOST=0.0.0.0
PORT=8088

# 日志级别：DEBUG / INFO / WARNING / ERROR
LOG_LEVEL=INFO

# 标量索引可选关闭（极小库可不建以省资源）
ENABLE_SCALAR_INDEX=1
